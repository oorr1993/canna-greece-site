# הפעלת מערכת קליטת הפניות המאובטחת — CanaFlight

המערכת כבר פרוסה באתר אבל **רדומה**: כל עוד לא הגדרת את משתני הסביבה, הטופס
ממשיך לעבוד בדיוק כמו קודם (שליחה ל-FormSubmit). ברגע שתשלים את 4 השלבים
למטה, הטופס יעבור אוטומטית לזרימה המאובטחת — בלי שינוי קוד נוסף.

מה המערכת נותנת:
- הנתונים והקבצים (דרכון/סלפי/מרשם) נשמרים במסד נתונים מוצפן שלך, לא בתיבת מייל.
- אחסון קבצים בדלי פרטי עם בקרת גישה.
- מחיקה אוטומטית אחרי 12 חודשים (אכיפה אמיתית של מדיניות הפרטיות).
- מייל התראה שמכיל רק מספר פנייה — בלי שום מידע אישי.
- הפרדה: אתה היחיד עם מפתח הגישה למידע הרגיש.

---

## שלב 1 — צור פרויקט Supabase (חינם)

1. היכנס ל-https://supabase.com והתחבר (GitHub / אימייל).
2. New project → תן שם (למשל `canaflight`), בחר אזור **EU (Frankfurt / Ireland)**
   כדי לשמור את המידע באיחוד האירופי, וקבע סיסמת מסד נתונים חזקה.
3. המתן ~2 דקות עד שהפרויקט מוכן.

## שלב 2 — הרץ את סקריפט ההקמה

1. בפרויקט: Project → SQL Editor → New query.
2. פתח את הקובץ `supabase/setup.sql` שבתיקיית האתר, העתק את כולו, הדבק, ולחץ **Run**.
   זה יוצר את הטבלה, נועל אותה (RLS), יוצר את דלי הקבצים הפרטי, ומגדיר את המחיקה
   האוטומטית אחרי 12 חודשים.

## שלב 3 — קח את שני המפתחות

בפרויקט: Project Settings → API. העתק:
- **Project URL** (למשל `https://abcd.supabase.co`)
- **service_role key** (תחת "Project API keys" → `service_role`, לחץ Reveal)

⚠️ מפתח ה-`service_role` הוא מפתח-על. אל תשים אותו בשום קובץ באתר ואל תשלח אותו
לאף אחד — הוא נכנס רק למשתני הסביבה של Vercel (שלב 4).

## שלב 4 — הוסף את המפתחות ל-Vercel

1. https://vercel.com → הפרויקט `canna-greece-site` → Settings → Environment Variables.
2. הוסף שני משתנים (Environment: Production):
   - `SUPABASE_URL` = ה-Project URL משלב 3
   - `SUPABASE_SERVICE_ROLE_KEY` = ה-service_role key משלב 3
3. Deployments → הפריסה האחרונה → ⋯ → **Redeploy** (כדי שהמשתנים ייכנסו לתוקף).

---

## איך בודקים שזה עובד

- אחרי ה-Redeploy, פתח `https://www.canaflight.com/api/health` — צריך להחזיר
  `{"configured":true}`.
- מלא פניית בדיקה בטופס. אמור להגיע מייל "פנייה חדשה (מערכת מאובטחת)" עם מספר פנייה
  בלבד, והרשומה המלאה + הקבצים יופיעו ב-Supabase → Table Editor → `submissions`
  ובדלי `intake-files`.

## איפה רואים את הפניות

Supabase → Table Editor → טבלת `submissions`. הקבצים: Storage → `intake-files`
(כל פנייה בתיקייה לפי מספר הפנייה). אפשר לסמן `handled = true` אחרי טיפול.

---

## שלב 5 (אופציונלי) — TikTok Events API (צד שרת)

הפיקסל של TikTok (בדפדפן) כבר פעיל בכל האתר, כולל אירוע המרה (`SubmitForm`)
שנורה בעמוד התודה כשמישהו שולח את השאלון. זה עובד כבר עכשיו, בלי שום הגדרה.

אם תרצה גם את השכבה השנייה (שליחה מהשרת, שלא נחסמת ע"י חוסמי פרסומות ומשפרת
דיוק שיוך) — נדרש מפתח נוסף:

1. TikTok Ads Manager → Assets → Events → הפיקסל שלך → Settings → API access
   → Generate access token.
2. Vercel → Settings → Environment Variables → הוסף `TIKTOK_ACCESS_TOKEN`
   עם הערך שקיבלת. Redeploy.
3. זה יתחיל לפעול **רק** אחרי שגם Supabase מוגדר (שלבים 1–4 למעלה) — כי
   שליחת האירוע מהשרת קורית מתוך `api/submit.js`, שרץ רק בזרימה המאובטחת.

---

## מה עדיין צריך ממך / מעורך הדין

הקוד נותן את **אמצעי האבטחה הטכניים**. עדיין באחריותך, מול עורך הדין:
1. **רישום מאגר מידע** אצל רשם מאגרי המידע (חובה כשאוספים מידע רפואי/ביומטרי).
2. אישור סופי שרמת האבטחה (Supabase EU + הצפנה + מחיקה) עונה על דרישות
   חוק הגנת הפרטיות ותיקון 13 עבור סוג המידע הזה.
3. עדכון סעיף 5 (אבטחת מידע) במדיניות הפרטיות לתיאור המדויק של המערכת החדשה —
   כרגע הסעיף מנוסח כללי; אחרי ההפעלה שווה לחדד אותו (אני יכול לעזור).
4. שקילת הצפנה נוספת של שדות רגישים במסד (column-level) אם עורך הדין ידרוש.
