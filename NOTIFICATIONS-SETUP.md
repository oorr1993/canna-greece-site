# הפעלת מערכת ההתראות על לידים — CanaFlight

המערכת בנויה בשלוש שכבות, וכל אחת מגנה על הקודמת:

| שכבה | מה היא עושה | מתי היא מצילה אותך |
|---|---|---|
| 1. טלגרם | push מיידי לנייד ברגע שנכנס ליד | תוך שניות מהפנייה |
| 2. מייל | אותה התראה, כגיבוי | טלגרם נפל / הטלפון כבוי |
| 3. רדיפה | מתריע שוב כל שעה על ליד שלא טופל | פספסת את שתי הראשונות |

**שכבה 3 היא הסיבה שלא תפספס ליד.** התראה בודדת אפשר להחמיץ; רדיפה שלא נעצרת עד שאתה מסמן `handled = true` — לא.

> כל הקוד **רדום** עד שתגדיר את משתני הסביבה. עד אז שום דבר לא נשבר, וכל התראה שדולגה נרשמת ב-`notification_log` — כדי שהפער יהיה גלוי, לא שקט.

⚠️ **חשוב לדעת לפני שמתחילים:** מערכת הקליטה כבר **חיה** (`/api/health` מחזיר `configured:true`). כלומר לידים נכנסים עכשיו. עד שתשלים את השלבים כאן, ההתראה היחידה שלך היא ה-relay הישן של FormSubmit — שדורש אישור חד-פעמי במייל ואין לו שום חיווי אם הוא מת. **תבדוק ב-Supabase → Table Editor → `submissions` אם יש שורות שלא קיבלת עליהן מייל.**

---

## מה נשלח בהתראה — ומה לא

ההתראות עוברות דרך טלגרם וג'ימייל, שניהם צד שלישי. לכן הן מכילות **רק**:

- מספר הפנייה (uuid אקראי — מזהה שורה, לא אדם)
- המסלול (VIP / מהיר / בסיסי)
- תאריך ההגעה ליוון

**לעולם לא נשלחים:** שם, אימייל, טלפון, מספר דרכון, עיר, ותיאור המצב הרפואי. אלה נשארים ב-Supabase מאחורי מפתח ה-`service_role`.

המסלול ותאריך ההגעה הם שני הנתונים היחידים שמשנים מה אתה עושה בשנייה הבאה — VIP מחייב תשובה תוך שעה. כל השאר נמצא בפאנל.

---

## שלב 1 — בוט טלגרם (3 דקות, חינם)

1. פתח טלגרם וחפש **@BotFather**.
2. שלח `/newbot`.
3. תן שם לבוט (למשל `CanaFlight Alerts`) ואז שם משתמש שמסתיים ב-`bot` (למשל `canaflight_alerts_bot`).
4. BotFather יחזיר לך **טוקן** בצורה `123456789:AAE...`. זה `TELEGRAM_BOT_TOKEN`.

### קבלת ה-chat id

1. חפש בטלגרם את הבוט שיצרת ולחץ **Start**, ושלח לו הודעה כלשהי (למשל `שלום`).
2. פתח בדפדפן, עם הטוקן שלך במקום `<TOKEN>`:
   `https://api.telegram.org/bot<TOKEN>/getUpdates`
3. חפש בתשובה `"chat":{"id":123456789`. **המספר הזה** הוא `TELEGRAM_CHAT_ID`.

> רוצה שההתראות יגיעו לכמה אנשים? צור קבוצה בטלגרם, הוסף אליה את הבוט, ושלח בקבוצה הודעה. ב-`getUpdates` ה-id של קבוצה הוא **שלילי** (למשל `-1001234567890`) — זה תקין, השתמש בו כמו שהוא.

---

## שלב 2 — סיסמת אפליקציה ל-Gmail (2 דקות, חינם)

זה מפעיל גם את מייל הגיבוי **וגם** את מייל האישור ללקוח, שכבר כתוב בקוד וממתין לאותו משתנה.

1. היכנס ל-https://myaccount.google.com/security עם `1cana.flight@gmail.com`.
2. הפעל **אימות דו-שלבי** אם עדיין לא פעיל (חובה — בלעדיו אי אפשר ליצור סיסמת אפליקציה).
3. גש ל-https://myaccount.google.com/apppasswords, צור סיסמה חדשה בשם `CanaFlight`.
4. תקבל 16 תווים. זה `GMAIL_APP_PASSWORD` (הזן ללא רווחים).

---

## שלב 3 — סוד ל-cron

צור מחרוזת אקראית ארוכה (32 תווים ומעלה). למשל בטרמינל:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

זה `CRON_SECRET`. הוא מונע ממישהו זר להפעיל את נקודת הקצה ולהציף אותך בהתראות.

---

## שלב 4 — הוספת המשתנים ל-Vercel

https://vercel.com → פרויקט `canna-greece-site` → **Settings → Environment Variables**.
הוסף את ארבעת המשתנים, כולם ב-Environment: **Production**:

| שם | ערך |
|---|---|
| `TELEGRAM_BOT_TOKEN` | הטוקן משלב 1 |
| `TELEGRAM_CHAT_ID` | ה-chat id משלב 1 |
| `GMAIL_APP_PASSWORD` | 16 התווים משלב 2 |
| `CRON_SECRET` | המחרוזת משלב 3 |

אופציונלי: `UNHANDLED_GRACE_HOURS` — כמה שעות ליד יכול להמתין לפני שהרדיפה מתחילה. ברירת המחדל 3.

לאחר מכן: **Deployments → הפריסה האחרונה → ⋯ → Redeploy** (משתני סביבה נכנסים לתוקף רק בפריסה חדשה).

---

## שלב 5 — הרצת ה-SQL

Supabase → **SQL Editor → New query**. פתח את `supabase/notifications.sql`, העתק הכול.

⚠️ **לפני Run:** מצא בקובץ את `REPLACE_WITH_CRON_SECRET` והחלף אותו ב-`CRON_SECRET` האמיתי משלב 3. אם השניים לא זהים בדיוק — כל הרצה תחזיר 401.

לחץ **Run**. זה יוצר את טבלת היומן, את ה-view לבדיקת תקינות, ואת המשימה שרצה כל שעה בין 09:00 ל-19:00 (שעון ישראל בקירוב).

---

## שלב 6 — בדיקה שהכול עובד

**א. בדיקת הבוט (מיידית):** החלף את הטוקן ואת ה-id ופתח בדפדפן:
```
https://api.telegram.org/bot<TOKEN>/sendMessage?chat_id=<CHAT_ID>&text=בדיקה
```
אם הגיעה הודעה בטלגרם — שכבה 1 מוכנה.

**ב. בדיקת הרדיפה:** בטרמינל, עם הסוד שלך:
```bash
curl -H "x-cron-secret: <CRON_SECRET>" https://www.canaflight.com/api/cron-unhandled
```
צפוי `{"ok":true,"pending":0}` אם אין לידים ממתינים, או התראה בטלגרם אם יש.

**ג. בדיקה מקצה לקצה:** מלא פניית בדיקה בטופס באתר. אמורות להגיע שלוש הודעות — טלגרם, מייל אליך, ומייל אישור ללקוח.

**ד. אחרי הבדיקה:** מחק את שורת הבדיקה ב-`submissions` (אחרת הרדיפה תנדנד עליה כל שעה), או סמן אצלה `handled = true`.

---

## תחזוקה שוטפת — שאילתה אחת לשמור

הרץ פעם בשבוע ב-SQL Editor:

```sql
select * from public.alert_health where leads > 0;
```

אם אתה רואה יום שבו `leads` גדול מאפס אבל `delivered_alerts` שווה לאפס — **ערוץ ההתראות שבור**, גם אם התיבה נראית שקטה ורגילה. זו בדיוק התקלה שהמערכת הישנה לא יכלה לגלות.

לבדיקת כשלים ספציפיים:

```sql
select created_at, kind, channels from public.notification_log
where not delivered order by created_at desc limit 20;
```

---

## איך זה בנוי (לעתיד)

| קובץ | תפקיד |
|---|---|
| `lib/notify.js` | שליחה לטלגרם ולמייל + רישום ביומן. גבול הפרטיות מתועד בראש הקובץ |
| `api/submit.js` | קורא ל-`notifyNewLead` אחרי שהליד נשמר. אף פעם לא נכשל בגלל התראה |
| `api/cron-unhandled.js` | נקודת הקצה של הרדיפה. מוגנת ב-`CRON_SECRET` |
| `supabase/notifications.sql` | טבלת יומן, view תקינות, ותזמון שעתי דרך `pg_cron` + `pg_net` |
| `vercel.json` → `crons` | גיבוי יומי ב-07:00 UTC. במסלול Hobby של Vercel אפשר cron אחד ליום בלבד — לכן הרדיפה השעתית רצה מ-Supabase, ו-Vercel רק חופר פעם ביום למקרה ש-`pg_net` כבוי בפרויקט. Vercel מוסיף אוטומטית `Authorization: Bearer $CRON_SECRET`, וזה מה שנקודת הקצה בודקת |

**שינוי חלון השעות:** ערוך את ה-cron ב-`notifications.sql` והרץ מחדש. `'0 6-16 * * *'` הוא שעתי בין 06:00 ל-16:00 UTC. ל-24/7 שנה ל-`'0 * * * *'`.
